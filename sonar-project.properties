# SonarQube Cloud project identity — no secrets here (tokens live in CI secrets).
# Consumed by the sonar-scanner for the standalone analysis path; the
# dotnet--analyse--sonarqube.yml workflow passes /k: and /o: explicitly.
# Keep sonar.projectKey in step with .sonarlint/connectedMode.json and the
# badge in the README.
sonar.projectKey=markstanden_coding-standards
sonar.organization=markstanden
sonar.sources=.