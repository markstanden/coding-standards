name: "Node Quality Check"

on:
  workflow_call:
    inputs:
      node-version:
        description: "Node.js version"
        type: string
        required: false
        default: "22"

      project-path:
        description: "Path to the npm project directory (relative to repository root)"
        type: string
        required: false
        default: "frontend"

      install-command:
        description: "Shell command to install dependencies"
        type: string
        required: false
        default: "npm ci"

      format-check-command:
        description: "Shell command to verify formatting (must exit non-zero on failure)"
        type: string
        required: false
        default: "npm run format:check"

      lint-check-command:
        description: "Shell command to verify linting (must exit non-zero on failure)"
        type: string
        required: false
        default: "npm run lint:check"

      run-format-check:
        description: "Whether to run the format check command"
        type: boolean
        required: false
        default: true

      run-lint-check:
        description: "Whether to run the lint check command"
        type: boolean
        required: false
        default: true

      compile-check-command:
        description: "Shell command to verify the project compiles (e.g. tsc --noEmit, dotnet build)"
        type: string
        required: false
        default: "npm run compile:check"

      run-compile-check:
        description: "Whether to run the compile check command"
        type: boolean
        required: false
        default: false

      test-check-command:
        description: "Shell command to run unit/component tests (must exit non-zero on failure)"
        type: string
        required: false
        default: "npm run test:check"

      run-test-check:
        description: "Whether to run the test check command"
        type: boolean
        required: false
        default: true

      cache-dependency-path:
        description: "Path to package-lock.json for npm cache (workspace repos use package-lock.json at root)"
        type: string
        required: false
        default: "package-lock.json"

      job-name:
        description: "Display name for the quality job in the Actions UI"
        type: string
        required: false
        default: "Node Quality Check"

      debug-output:
        description: "Enable verbose debug output to aid troubleshooting"
        type: boolean
        required: false
        default: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  quality-check:
    name: ${{ inputs.job-name }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
          cache-dependency-path: ${{ inputs.cache-dependency-path }}

      - name: Install dependencies
        working-directory: ${{ inputs.project-path }}
        env:
          INSTALL_CMD: ${{ inputs.install-command }}
        run: bash -c "$INSTALL_CMD"

      - name: Format check
        if: inputs.run-format-check
        working-directory: ${{ inputs.project-path }}
        env:
          FORMAT_CMD: ${{ inputs.format-check-command }}
        run: bash -c "$FORMAT_CMD"

      - name: Lint check
        if: inputs.run-lint-check
        working-directory: ${{ inputs.project-path }}
        env:
          LINT_CMD: ${{ inputs.lint-check-command }}
        run: bash -c "$LINT_CMD"

      - name: Compile check
        if: inputs.run-compile-check
        working-directory: ${{ inputs.project-path }}
        env:
          COMPILE_CMD: ${{ inputs.compile-check-command }}
        run: bash -c "$COMPILE_CMD"

      - name: Test check
        if: inputs.run-test-check
        working-directory: ${{ inputs.project-path }}
        env:
          TEST_CMD: ${{ inputs.test-check-command }}
        run: bash -c "$TEST_CMD"

      - name: Debug package scripts
        if: inputs.debug-output
        working-directory: ${{ inputs.project-path }}
        run: npm run
